<div class="metric-card @CssClass">
    @if (!string.IsNullOrEmpty(Icon))
    {
        <div class="metric-card__icon">@Icon</div>
    }
    <div class="metric-card__body">
        <span class="metric-card__label">@Label</span>
        <span class="metric-card__value">@Value</span>
        @if (!string.IsNullOrEmpty(Delta))
        {
            <span class="metric-card__delta">
                @if (DeltaPositive.HasValue)
                {
                    <span class="metric-card__arrow">@(DeltaPositive.Value ? "▲" : "▼")</span>
                }
                @Delta
            </span>
        }
    </div>
</div>

@code {
    [Parameter, EditorRequired] public string Label { get; set; } = "";
    [Parameter, EditorRequired] public string Value { get; set; } = "";
    [Parameter] public string Delta { get; set; } = "";
    [Parameter] public bool? DeltaPositive { get; set; }
    [Parameter] public string Icon { get; set; } = "";

    private string CssClass => DeltaPositive switch
    {
        true => "metric-card--up",
        false => "metric-card--down",
        _ => ""
    };
}
